spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/starter
    username: postgres
    password: postgres

app:
  storage:
    s3-bucket-name: test-bucket
    s3-region: eu-central-1
  email:
    enabled: false

logging:
  level:
    com.starter: DEBUG
