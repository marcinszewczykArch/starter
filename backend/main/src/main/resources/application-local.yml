spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/starter
    username: postgres
    password: postgres

logging:
  level:
    com.starter: DEBUG

# Disable email sending locally (no AWS credentials)
app:
  email:
    enabled: false
